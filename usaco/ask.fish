echo ```cpp > ~/tmp/__1.txt
cat $argv >> ~/tmp/__1.txt
echo ``` >> ~/tmp/__1.txt
echo '下面是我的代码风格（必须加上些许注释，少空行，变量名简短而有意义），根据我的重写上面' >> ~/tmp/__1.txt
echo ```cpp >> ~/tmp/__1.txt
cat ./usaco-2026-first-contest/2.cpp >> ~/tmp/__1.txt
echo ``` >> ~/tmp/__1.txt


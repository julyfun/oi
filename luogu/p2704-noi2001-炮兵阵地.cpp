#include <iostream>
#include <bitset>
using namespace std;
const int N = 1e2 + 10;
int n, m;
string a[N];
int main() {
    for (int i = 1; i <= n; i++) cin >> a[i];
    // 2: 放了
    // 1: 上面放了
    // 0: 上上放了(不影响下面)
    return 0;
}
